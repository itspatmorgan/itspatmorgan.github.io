<footer class="py-8 sm:py-10">
  <div class="mx-auto flex w-full max-w-[1400px] items-center justify-between px-6 sm:px-8 lg:px-14">
    <a
      href="/colophon"
      class="font-mono text-[10px] uppercase tracking-widest text-muted-foreground/60 transition-colors hover:text-foreground"
    >
      Colophon
    </a>
    <div class="flex items-center gap-3 whitespace-nowrap font-mono text-[10px] uppercase tracking-widest text-muted-foreground/50">
      <span>&copy; {new Date().getFullYear()}</span>
      {/* Sidebar already shows local time on desktop. */}
      <span class="md:hidden">
        <span class="font-mono text-xs text-muted-foreground tabular-nums whitespace-nowrap">
          LA&nbsp;&middot;&nbsp;<span data-footer-local-time>--:--</span>
        </span>
      </span>
    </div>
  </div>
</footer>

<script>
  function initFooterLocalTime() {
    document.querySelectorAll<HTMLElement>("[data-footer-local-time]").forEach((time) => {
      if (time.dataset.initialized === "true") return;
      time.dataset.initialized = "true";

      function updateTime() {
        time.textContent = new Date().toLocaleTimeString("en-US", {
          timeZone: "America/Los_Angeles",
          hour: "numeric",
          minute: "2-digit",
          hour12: true,
        });
      }

      updateTime();

      const now = new Date();
      const msUntilNextMinute = (60 - now.getSeconds()) * 1000 - now.getMilliseconds();
      window.setTimeout(() => {
        updateTime();
        window.setInterval(updateTime, 60_000);
      }, msUntilNextMinute);
    });
  }

  initFooterLocalTime();
  document.addEventListener("astro:after-swap", initFooterLocalTime);
</script>
